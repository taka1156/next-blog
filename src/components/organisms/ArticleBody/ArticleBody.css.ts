import { style } from '@vanilla-extract/css';

export const styles = {
  articleBody: style({
    marginTop: 5,
    marginRight: 'auto',
    marginLeft: 'auto',
    marginBottom: 'auto',
    textAlign: 'left',
    wordWrap: 'break-word'
  })
};
