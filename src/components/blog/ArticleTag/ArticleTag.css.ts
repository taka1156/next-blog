import { style } from '@vanilla-extract/css';

export const styles = {
  articleTag: style({
    display: 'flex',
    flexWrap: 'wrap',
    alignItems: 'center',
    margin: 10,
    padding: 8
  })
};
